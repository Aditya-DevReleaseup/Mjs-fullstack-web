# Frontend production
VITE_APP_ENV=production

# Use relative /api so Nginx can proxy it to backend at localhost:3000
# This avoids CORS and TLS complications (browser calls same origin).
VITE_API_URL="https://mylive-web.devclouds.site/api/"



